#include "GaussJordanElimination.hpp"

std::vector<double> GaussJordanElimination::apply(
    const std::vector<std::vector<double>>& x,
    const std::vector<double>& y
) const
{
    auto result = std::vector<double>(y.size());

    return result;
}